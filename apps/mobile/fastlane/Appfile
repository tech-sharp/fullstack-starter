app_identifier(ENV["APP_IDENTIFIER"] || "com.example.mobile")
apple_id(ENV["APPLE_ID"])
itc_team_id(ENV["ITC_TEAM_ID"])
team_id(ENV["TEAM_ID"])

for_platform :android do
  json_key_file(ENV["GOOGLE_PLAY_JSON_KEY_PATH"])
  package_name(ENV["APP_IDENTIFIER"] || "com.example.mobile")
end
